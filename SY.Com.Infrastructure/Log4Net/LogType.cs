namespace SY.Com.Infrastructure
{
    public enum LogType : byte
    {
        ERROR = 0x01,
        WARN,
        INFO,
        DEBUG,
        EXPANDB,
        EXPANDC,
        EXPANDD,
        EXPANDE
    }
}
